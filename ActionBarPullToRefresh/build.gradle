apply plugin: 'com.android.library'

android {
    compileSdkVersion verMaxSdk
    buildToolsVersion verBuildTools

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            res.srcDirs = ['res']
        }
    }
}